---
interface Props {
  href: string;
  variant?: "primary" | "secondary";
}

const { href, variant = "primary" } = Astro.props;
---

<a
  href={href}
  class:list={[
    "btn-ritual",
    variant === "primary" ? "btn-ritual-primary" : "btn-ritual-secondary",
  ]}
>
  <slot />
</a>
